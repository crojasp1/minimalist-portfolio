---
import Section from '@components/Section.astro';
import Experience from '@components/Experience.astro';

const { title, experiences } = Astro.props;
---

<Section title={title}>
	{!!experiences?.length && experiences.map((experience) => <Experience {...experience} />)}
</Section>
