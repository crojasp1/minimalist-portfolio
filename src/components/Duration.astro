---
export interface Props {
	from: string;
	to: string;
}

const { from, to } = Astro.props;
---

<span>{`${from} - ${to}`}</span>

<style>
	span {
		font-size: 0.8rem;
		font-weight: 500;
	}
</style>
