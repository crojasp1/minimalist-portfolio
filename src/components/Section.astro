---
const { title } = Astro.props;
---

<section>
	<h2>{title}</h2>
	<slot />
</section>
<style lang="scss">
	@use '@scss/index' as *;

	section {
		max-width: $width-page;
		margin-bottom: 1rem;

		h2 {
			font-size: 1.5rem;
			margin-bottom: 0.5rem;
			font-weight: bold;
		}
	}
</style>
